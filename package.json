{
  "name": "sp500-sector-heatmap-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Dashboard heatmap del S&P 500 por sectores para Cloudflare Workers.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev --local",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
